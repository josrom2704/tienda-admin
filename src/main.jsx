import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App';
import './index.css';

// Punto de entrada principal para React. Renderiza la aplicación dentro del
// elemento #root y envuelve la aplicación con BrowserRouter para habilitar
// enrutamiento.
ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);